**Figure 1.** PCS conditional entropy. Lower H(suffix|core) relative to H(suffix|prefix) and H(core|prefix) supports asymmetric internal organization centered on the operationally defined core.
**Figure 2.** PCS generation matching rate. The structured PCS model reproduces observed token forms at a substantially higher rate than the random baseline.
**Figure 3.** Token mass coverage. The structured PCS model captures much more observed token mass than the random baseline.
**Figure 4.** Zipf slope difference. Lower difference indicates closer agreement with the observed rank-frequency profile; the structured PCS model is closer to the real corpus than the random baseline.
**Figure 5.** Compression ratio comparison. The real and PCS-generated corpora are more compressible than shuffled text, supporting structural regularity without implying direct semantic reading.
**Figure 6.** Inter-token structure. Real sequences show lower bigram entropy and higher next-token prediction accuracy than the line-internal shuffled baseline, supporting non-random local arrangement.
